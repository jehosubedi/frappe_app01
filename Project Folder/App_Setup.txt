Steps in setting up the app

Download the folder

Open the file on Visual Studio Code

Use "pip install -r requirements.txt" to download the necessary dependencies

To Run type "python app.py"
